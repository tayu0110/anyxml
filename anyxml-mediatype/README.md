# anyxml-mediatype
`anyxml-mediatype` is a simple media type library for XML.

This crate provides the minimum features required for `anyxml` crate.  \
If you need a more powerful library, I recommend `mime` crate.
